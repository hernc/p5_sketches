var doc = app.activeDocument;for (var i = 0; i < doc.selection.length; i++) {	var item = doc.selection[i];	$.writeln(item);	$.writeln(item.constructor.name);	item.rotate(45);	// Displace points in Path Items	// if (item instanceof PathItem) {			// 	$.writeln("Found a Path Item");	// 	for (var p = 0; p < item.pathPoints.length; p++) {	// 		// get the pathPoint 	// 		var pathPoint = item.pathPoints[p];	// 		// read the x and y, and modify them	// 		var x = pathPoint.anchor[0];	// 		var y = pathPoint.anchor[1];	// 		x += Math.random() * 10 - 5;	// 		y += Math.random() * 10 - 5;	// 		// move the anchor of the path point	// 		pathPoint.anchor = Array(x, y);				// 		// move the curve handles to the point (removes curviness)	// 		pathPoint.leftDirection = pathPoint.anchor;	// 		pathPoint.rightDirection = pathPoint.anchor;	// 	}	// }}